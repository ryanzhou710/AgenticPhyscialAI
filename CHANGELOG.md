# 修改记录

## 2026-09-20 — 增加 OpenAI API Key 调用方式

- 增加 `api_key` 认证模式，保留 Codex OAuth 作为默认模式。
- API Key 从 `OPENAI_API_KEY` 环境变量读取，不进入运行配置、审计记录或命令行参数。
- 增加标准 OpenAI Responses API transport，继续支持图片输入、流式输出和结构化 JSON 响应。
- 增加 CLI 参数 `--auth-mode api_key`，用于选择 API Key 调用方式。
- 增加 API Key 请求头、认证模式选择和缺少环境变量的单元测试。
- 更新 README，补充 API Key 的配置方法和安全注意事项。

## 2026-09-20 — 缺少 OAuth 凭据时自动启动 Codex 登录

- 模型客户端找不到可读取的 Codex OAuth 缓存时，自动调用 `codex login`。
- 登录过程交给 Codex CLI 处理，用户在浏览器中完成授权后，程序重新读取凭据并继续运行。
- 支持通过 `FOAMAGENT_CODEX_DEVICE_AUTH=1` 使用设备码登录，适用于无本地浏览器或 localhost 回调受阻的环境。
- 登录失败、未安装 Codex CLI 或登录后仍找不到 `auth.json` 时，输出明确错误，不会读取或打印用户密码和访问令牌。
- README 增加首次登录、设备码登录、凭据文件位置和安全注意事项。
- 增加单元测试，覆盖浏览器登录触发和设备码登录失败处理。

## 2026-09-19 — 支持任意形状进出口

### 问题原因

- `spaceclaim_build_v241.py` 只接受圆形 `Circle` 边，或只包含一个圆形内环的平面面。
- 矩形出口等几何会因为“不是圆形”或“没有一个圆形内环”而在体积抽取阶段失败。
- 后续边界分组还使用 `πr²` 和圆心匹配出口面，因此即使抽取成功也无法可靠识别非圆形出口。

### 本次修改

- 支持平面面上的任意闭合轮廓：矩形、多边形、圆形、圆弧/直线/样条混合轮廓均可由多个边组成。
- 支持直接选择闭合 `loop`；对于齐平端面，支持使用唯一的外环作为进出口边界。
- 保留圆形边的兼容性；半径只作为圆形的可选信息，不再是开口识别的必要条件。
- 体积抽取改为把完整轮廓的所有边传入 `VolumeExtract`，不再只传一个边。
- 边界分组改用轮廓中心、法向、周长和可用的面积联合匹配；一般非圆形内环不再假设存在半径。
- 查询阶段增加 `loop` 候选，并更新模型选择提示，指导模型优先选择平面面或闭合环。
- 更新 README，明确项目支持任意平面开口轮廓。

### 兼容性与边界

- 原有圆形开口流程保持兼容。
- 一个平面面若包含多个内环，仍会被判定为不唯一，需要用户选择具体的闭合环。
- 单条非闭合边不能定义一个完整进出口；这种情况需要在 SpaceClaim 中选择对应的闭合 loop 或平面面。

### 验证

- 增加/更新离线单元测试，覆盖任意形状开口候选的目录描述。
- 在没有 Ansys/SpaceClaim 运行环境时，只能完成 Python 侧静态检查；最终仍建议用一个圆形和一个矩形/多边形开口各跑一次完整 Fluent 网格流程。

## 2026-09-20 — 修正 Volume Extract 种子面传递

- 修复 `VolumeExtract.Create` 调用中第二个选择参数为空的问题。
- 现在会把模型确认的 `seed_inner_wall_id` 作为明确的 Seed Face 传入，同时保留种子点选项。
- 提取失败时额外记录成功状态、创建体数量、封口边数量和种子面 ID，便于定位 CAD 拓扑问题。

## 2026-09-20 — 兼容齐平端面封口并加强拓扑诊断

- 对只有唯一外环的齐平平面端面，优先按端面选择交给 SpaceClaim 封口，适配矩形、多边形等多边界边端口。
- 对带内环的支撑面继续使用明确的边集合，避免把支撑面的外环误当成开口边界。
- 当前测试几何仍报告“至少有一个端部未被封闭”；日志已确认代码能够识别圆形入口和矩形出口，但该 `.scdoc` 的两个候选端面属于实心体的外环，未形成可提取的内部流体腔体。

## 2026-09-24 — 恢复默认 Volume Extract 规则

- 闭合正体积实体只能说明拓扑可复用，不能单独证明该实体是流体域。
- 恢复默认执行 Volume Extract；只有 Prompt 明确声明输入已经是流体域且拓扑校验通过时，才跳过抽取。
- 保留已有流体域路径中的单实体、正体积、无自由边和 seed face 归属检查；声明与拓扑不一致时记录失败并交给 Reviewer。

## 2026-09-20 — 已有实体流体体积时跳过 Volume Extract

- 增加实体体积检测：当输入只有一个正体积、拓扑闭合且无自由边的 solid body 时，直接把它作为流体域。
- 该分支跳过 `VolumeExtract.Create`，保留入口/出口/壁面命名，然后进入 Fluent Meshing。
- 对 sheet body、多个实体体积或含自由边的模型，继续使用原有的封口与 Volume Extract 流程。
- 是否跳过由 SpaceClaim 的实体数、体积和自由边拓扑决定；提示词只用于帮助 LLM 选择边界，不能覆盖该安全检查。
- 增加单元测试，覆盖“闭合实体直接进入命名”和“sheet/开放拓扑继续抽取”两种情况。
